// Linear Search
// Ketentuan:

// Mencari data pada array
// Buat fungsi yang menerima parameter angka yg ingin dicari dan data array
// Kembalikan dalam bentuk boolean
let = [8, 3, 9, 2, 5];
let temukan = 0;
function angkass(angka) {
  for (let angka = angka0; angka < angka.length; index++) {
    const element = angka[index];
  }
}
console.log(angkass([8, 3, 9, 2, 5]));
